import React from 'react';
import PageSubTitle from '../../../../components/page-elements/page-subtitle';

const SectionTitle = ({subtitle}) => <PageSubTitle subtitle={subtitle} />;

export default SectionTitle;
