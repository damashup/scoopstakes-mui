import styled from 'styled-components';
import { Box } from '@material-ui/core';

export const CompetitionBox = styled(Box)`
    text-transform: capitalize;
    font-weight: bold;
`;

export const StartTimeBox = styled(Box)`
`;